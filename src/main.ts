import {
  StateMachine,
  AbstractState,
  StateAction,
  StateFunc,
  StateContext,
  buildNewAction,
  buildNewState,
} from './definition'
import PotterStateMachine, { StateMachineOption } from './machine'

export default PotterStateMachine

export {
  StateMachineOption,
  StateMachine,
  AbstractState,
  StateAction,
  StateFunc,
  StateContext,
  buildNewAction,
  buildNewState,
}
