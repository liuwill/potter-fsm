import {
  StateMachine,
  AbstractState,
  StateAction,
  StateFunc,
  StateContext,
  buildNewAction,
  buildNewState,
} from './definition'
import PotterStateMachine, { StateMachineOption } from './machine'

export default PotterStateMachine

export {
  PotterStateMachine,
  StateMachineOption,
  StateMachine,
  AbstractState,
  StateAction,
  StateFunc,
  StateContext,
  buildNewAction,
  buildNewState,
}
